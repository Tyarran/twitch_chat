defmodule TwitchHandlerTest do
  use ExUnit.Case, async: true
  # import Mox

  # alias TwitchChat.TwitchHandler

  # describe "TwitchHandler.init" do
  #   test "initial" do
  #     # client_id = "test_client_id"
  #     # client_secret = "test_client_secret"
  #     state = %{handler: nil, client: nil, pending_messages: []}
  #     expect(TwitchChat.MockTwitchHandler, :start_link, fn _nick -> {:ok, :quelque_chose} end)
  #
  #     result = TwitchHandler.init(state)
  #
  #     assert result == {:ok, %{handler: nil, client: :quelque_chose, pending_messages: []}}
  #   end
  # end
end
